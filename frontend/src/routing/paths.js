const HOME = '/'

const LOGIN = '/login'

const REGISTER = '/register'

const RESET_PASSWORD = '/reset/:token'

const FORGOT_PASSWORD = '/forgot-password'

export default {
  LOGIN,
  REGISTER,
  RESET_PASSWORD,
  HOME,
  FORGOT_PASSWORD,
}
